# المساهمة في المشروع

شكراً لمساعدتك في تحسين بيانات المدن والأقضية العراقية.

## قبل تعديل البيانات

- تحقق من التغيير باستخدام مصدر موثوق، واذكر المصدر في وصف طلب السحب.
- حافظ على أسماء الحقول الحالية وترميز UTF-8.
- أبقِ الأسماء العربية كما تم التحقق منها، إلا إذا كان التغيير تصحيحاً
  إملائياً محدداً.
- عند إضافة أو تعديل محافظة، حافظ على اتساق `id` و`key` و`iso_code` في
  `cities.json` واسم ملف الأقضية وقيمة `shapeISO` المطابقة في GeoJSON.
- حافظ على صحة ملف GeoJSON كـ `FeatureCollection`، وعلى ترتيب الإحداثيات
  القياسي `[longitude, latitude]`.
- لا تستبدل معرفات المزودين `null` بسلاسل فارغة أو قيم مخترعة.
- لا تعِد استخدام معرف موجود.

## الفحوص المحلية

شغّل الفاحص من جذر المستودع:

```bash
npm run validate
```

يفحص الفاحص صيغة JSON والحقول المطلوبة وأسماء الملفات والمفاتيح الأجنبية
وتكرار معرفات الأقضية. ولا يتحقق حالياً من طوبولوجيا GeoJSON أو مصدر الحدود
الخارجي، لذلك يجب مراجعة تغييرات الحدود بعناية.

## تغييرات الواجهة والترجمة

الصفحة الوحيدة هي `index.html`. يجب إبقاء منطق الخريطة والبيانات والبحث في
`shared.js` والأنماط المشتركة في `shared.css`. عند تعديل النصوص، حافظ على
تزامن العربية والإنجليزية والكردية السورانية، وأضف اللغة الجديدة إلى سجل
اللغات بدلاً من إنشاء صفحة HTML منفصلة.

## طلبات السحب

اشرح ما الذي تغير ولماذا، واذكر مصدر التحقق، وبيّن إن كان هناك أثر على ربط
مزودي الخدمة أو مطابقة GeoJSON. أبقِ تغييرات التنسيق غير المتعلقة بالموضوع
والملفات المولدة خارج طلب السحب.
