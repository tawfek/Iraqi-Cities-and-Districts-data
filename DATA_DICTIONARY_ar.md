# قاموس البيانات

[English](DATA_DICTIONARY.md) | [العربية](DATA_DICTIONARY_ar.md) | [کوردی](DATA_DICTIONARY_ku.md)

## `data/cities.json`

مصفوفة من سجلات المحافظات.

| الحقل | النوع | الوصف |
| --- | --- | --- |
| `id` | integer | المعرف الداخلي للمحافظة. |
| `name` | string | اسم العرض باللغة العربية. |
| `key` | string | رمز لاتيني من ثلاثة أحرف يستخدم في أسماء الملفات والبحث. |
| `iso_code` | string | رمز تقسيم إداري بصيغة ISO، مثل `IQ-NA`، ويستخدم لربط GeoJSON. |
| `country_id` | integer | معرف الدولة؛ قيمته الحالية `1`. |
| `ebhar_id` | UUID/null | معرف التكامل مع Ebhar. |
| `alwaseet_id` | integer | معرف التكامل مع Alwaseet. |

## ملفات أقضية المحافظة

تستخدم الملفات الصيغة `<city-id>-<city-key>.json`، مثل `1-NJF.json`، وتحتوي
على الأقضية والنواحي التابعة للمحافظة.

| الحقل | النوع | قابل للقيمة الفارغة | الوصف |
| --- | --- | --- | --- |
| `id` | integer | لا | معرف سجل القضاء. |
| `name` | string | لا | اسم القضاء أو الناحية باللغة العربية. |
| `state_id` | integer | لا | مفتاح أجنبي يساوي `cities.json.id`. |
| `is_prime_supported` | boolean | لا | هل القضاء مدعوم من Prime. |
| `prime_id` | integer | لا | معرف Prime المحفوظ من المصدر. |
| `ebhar_id` | UUID/null | نعم | معرف Ebhar الاختياري. |

## ملف الحدود الجغرافية

`data/geoBoundaries-IRQ-ADM1_simplified.geojson` هو `FeatureCollection` من
GeoJSON لحدود المحافظات المبسطة في المستوى ADM1.

| الخاصية | النوع | الوصف |
| --- | --- | --- |
| `shapeName` | string | اسم المحافظة من مصدر الحدود. |
| `shapeISO` | string | رمز يستخدم لمطابقة `cities.json.iso_code`. |
| `shapeID` | string | معرف الشكل في مصدر الحدود. |
| `shapeGroup` | string | رمز الدولة/المجموعة، وقيمته الحالية `IRQ`. |
| `shapeType` | string | المستوى الإداري، وقيمته الحالية `ADM1`. |

تستخدم إحداثيات GeoJSON الترتيب القياسي `[longitude, latitude]`. ملف الحدود
للعرض الجغرافي ولا يحتوي على بيانات الأقضية.

## قواعد العلاقة

1. يجب أن تساوي قيمة `state_id` في كل قضاء قيمة `id` لمحافظته.
2. يجب أن يطابق الرقم والرمز في اسم الملف قيمتي `id` و`key` للمحافظة.
3. يجب أن تكون معرفات الأقضية فريدة على مستوى مجموعة البيانات.
4. يجب أن تكون ملفات JSON بترميز UTF-8 وبمصفوفة في المستوى الأعلى.
5. يستخدم التطبيق `shapeISO` لربط حدود GeoJSON مع `cities.json.iso_code`.

معرفات مزودي الخدمة هي مفاتيح تكامل وليست إحداثيات جغرافية، وقد تعتمد
صلاحيتها على المزود الخارجي.

## ملفات الواجهة المشتركة

`index.html` هي الصفحة التفاعلية الوحيدة. يحتوي `shared.js` على منطق الخريطة
وتحميل البيانات والبحث وتبديل اللغات والسمات والبطاقات. يحتوي `shared.css`
على الأنماط المشتركة والوضعين الداكن والفاتح.
